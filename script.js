
let humanScore = 0;

let computerScore = 0;


function getComputerChoice () {} // which returns either rock, paper, scissors using the Math.random method


function getHumanChoice () {} // use the prompt method to get user's input


function playGame () { // This function can call playRound several times, depending on the amount of rounds they entered to play
    function playRound (humanChoice, computerChoice) {}
}
